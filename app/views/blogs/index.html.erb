<% provide(:title, "Word From The Pastors") %>
<h4 class="core-title text-center">Our Blogs</h4>

<div class="blog-list">

Our blog has <%= Blog.public_count %> blogs and counting!

<ul>
  <% @blogs.each do |blog| %>
  <% unless blog.archived? %>
    <li>
      <%= link_to blog.title, blog %>
    </li>
  <% end %>
  <% end %>
</ul>

<% if session[:user_id] %>
	<%= link_to "New Blog", new_blog_path %>
<% end %>
</div>
